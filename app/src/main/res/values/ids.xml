<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Pre-defined IDs allowing forward ID references for RelativeLayout of statistics table. -->
    <item name="statsTable" type="id"/>

    <item name="horizontalDivider02" type="id"/>
    <item name="horizontalDivider01" type="id"/>

    <item name="verticalDivider01" type="id"/>
    <item name="verticalDivider02" type="id"/>
    <item name="verticalDivider03" type="id"/>

    <!--
    Some problems in the layout editor if (maybe) an ID is defined with "@+id/..." in a custom
    attribute.
    -->
    <item name="solve_hints_panel"   type="id"/>

</resources>
